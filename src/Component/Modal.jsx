function Modal({ projectTitle }) {
  return (
    <div className="modal">
      <p className="modal-title">{projectTitle}</p>
      <p className="click-button">Click</p>
    </div>
  );
}

export default Modal;
